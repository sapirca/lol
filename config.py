
config = {
    
    "use_stub": True,
    "selected_backend": "GPT",  # Specify which backend to use (or None for random selection)
    "should_add_knowledge": True  # Append knowledge prompts to the main prompts
}
